public class Inheritance9 {
}
/*
Which of the following statements are correct?
Answered Incorrectly

You had to select 3 option(s)

An abstract class can be extended by an abstract or a concrete class. // TRUE

A concrete class can be extended by an abstract or a concrete class. // TRUE
An abstract class always extends a concrete class (java.lang.Object at the very least).

An interface can be extended by another interface. // TRUE

An interface can be extended by an abstract class. // FALSE
A class "implements" an interface. It does not "extend" an interface.

An interface can be extended by a concrete class. // FALSE
A class "implements" an interface. It does not "extend" an interface.

An abstract class cannot implement an interface. // FALSE
Any class, whether abstract or concrete, can implement any interface.
 */